.container-admin
  .publisher-content
    h2.pub-title
      | Manage Publisher
    .publisher-but
      = render "add"
      = render "export"
    = render "search"
    = render "head"
    - flash.each do |type, msg|
      .alert
        = msg
    - if @publishers.length > 0
      = render @publishers
    - else
      h2 
        | Khong co ket qua nao!!!
    .center_pag
      = will_paginate @publishers
