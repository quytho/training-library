.container-admin
  .publisher-content
    h2.pub-title
      | Manage Books
    .publisher-but
    .publisher-but
      = render "add"
      = render "export"
    = render "search"
    = render "head"
    = render @books
    .center_pag
      = will_paginate @books
